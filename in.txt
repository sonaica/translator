import "math.h";
define for fon;

struct cool_struct {};

main() {
    int a[5];
    int x;
    input a, x;
    int l = -1, r = 5;
    while (r - l > 1) {
        int mid = l + (r - l) / 2;
        if (x > a[0]) {
            l = mid;
        } else {
            r = mid;
        }
    }
    if (a[0] == x) {
        output "Haleluyah";
    } else {
        output "aw man";
    }
}