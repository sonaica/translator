import "catalog/name";

struct my_struct{
    int x, y;
}

fun main(){
    int a, b; // some text
    a += a*(a *= b) + 345;

    for (int i = 0; i < n; i++) {
        if (i < a) {
            output i, a, b;
        }
    }

    while (b *= a > 3) do {
        input a, b;
    }
    // output '\n', "asdfsa";
    return;
}