import "math.h";
define for forn;

struct cool_struct {} bob;

main() {
    int a[5];
    int x;
    input a, x;
    int l = -1, r = 5;
    while (r - l > 1) {
        int mid = l + (r - l) / 2;
        if (x > a) {
            l = mid;
        } else {
            r = mid;
        }
    }
    if (a == a[x]) {
        output "Haleluyah";
    } else {
        output "aw man";
    }
}